export interface LinkType {
  name: string,
  src: string,
}

export interface CardType {
  imgAlt: string,
  imgSrc: string,
  text: string,
  title: string,
}

// src/types.ts
export type Direction = "left" | "right" | "up" | "down";

